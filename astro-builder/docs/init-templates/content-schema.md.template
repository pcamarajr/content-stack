# Content Schema

Defines all content types in this project, their fields, relationships, and i18n strategy.
Generated by `astro-builder`. Update by re-running `/astro-builder:init` or editing directly.

## Content types

{{#each CONTENT_TYPES}}
### {{name}}

**Collection name**: `{{collectionName}}`
**Folder**: `src/content/{{collectionName}}/`
**Translatable**: {{translatable}}
**URL pattern**: `/{locale}/{{urlPattern}}/{slug}`

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `title` | `string` | yes | Display title |
| `description` | `string` | yes | Short summary (used in meta and cards) |
| `date` | `date` | yes | Publication date (ISO format) |
| `tags` | `string[]` | no | Flexible tags (no fixed enum) |
| `lang` | `enum` | yes | Locale code (`en`, `it`, etc.) |
| `translationKey` | `string` | no | Links the same content across locales |
| `draft` | `boolean` | no | `true` = excluded from production build |
{{#each customFields}}
| `{{name}}` | `{{type}}` | {{required}} | {{description}} |
{{/each}}

#### Relationships

{{relationships}}

{{/each}}

## Cross-collection relationships

{{CROSS_COLLECTION_RELATIONSHIPS}}

## Frontmatter example

```markdown
---
title: "Example title"
description: "A short description for meta tags and cards."
date: 2026-01-15
tags: ["tag-one", "tag-two"]
lang: en
translationKey: "my-unique-key"
draft: false
---

Content body starts here...
```

Markdown bodies are rendered by Astro 7's built-in processor — GFM, smart punctuation, and heading IDs work with no plugin configuration.

## i18n strategy

- Each translatable content type has locale subfolders: `en/`, `it/`, etc.
- Files use the same slug across locales, identified by `translationKey`.
- The `lang` field is the canonical locale identifier — never parse the path.
- `draft: true` entries are excluded from `getStaticPaths()` in production.
