{
  "name": "content-stack",
  "private": true,
  "description": "Tooling-only manifest: commit-message linting for the plugin marketplace. The plugins themselves are not npm packages.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.2",
    "@commitlint/config-conventional": "^21.0.2",
    "husky": "^9.1.7"
  }
}
